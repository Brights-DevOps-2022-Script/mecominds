apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-conf
  namespace: mecominds
data:
  nginx.conf: |
    worker_processes 1;
    events {
      worker_connections 1024;
    }
    http { 
      server {
        location /stub_status {
          stub_status on;
          access_log off;
        }
        location / {
          root   /usr/share/nginx/html;
          index  index.html index.htm;
        }
      }
    }
