apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  labels:
    app: nginx         
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:         
      labels:
        app: nginx      
    spec:               
      containers:
      - name: nginx
        image: devops2022.azurecr.io/nevermindset:e5a0fc3947c338f53677860e60f7b6975ebeb59e
        ports:
        - containerPort: 80  
        resources:
          limits:
            cpu: 100m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata: 
  name: service
spec: 
  selector: 
    app: nginx
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress               
metadata:
  name: ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/force-ssl-redirect: 'true'
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - devops2022.de
    secretName: tls
  rules:
  - host: devops2022.de                       
    http:                    
      paths:
      - path: /neverMindset                      
        pathType: Prefix
        backend:
          service:
            name: service
            port:
              number: 80         
